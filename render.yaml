services:
  - type: web
    name: skiller-smartbank
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn Application:app --bind 0.0.0.0:$PORT
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PYTHON_VERSION
        value: "3.9.18"
      - key: SMTP_SERVER
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "465"
      - key: SMTP_USER
        value: banksupport95@gmail.com
      - key: SMTP_PASSWORD
        value: eydgeaianfmvznsh
